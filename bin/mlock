#!/bin/bash

TMP="$(mktemp /tmp/scrot.XXXXXXXX.png)"
scrot "$TMP"

convert "$TMP"   \
	-scale 25%   \
	-blur 0x4    \
	-scale 400%  \
	"$TMP"

composite -gravity center ~/assets/img/lock.png "$TMP" "$TMP"

i3lock -i "$TMP" -p default -n

rm "$TMP"
